/* ==========================================================
   PARTS — catalog. Filled in Chunk 2.
   ========================================================== */
(function (global) {
  'use strict';
  global.Parts = {
    all: [],
    byId() { return null; },
    byCategory() { return []; },
  };
})(window);

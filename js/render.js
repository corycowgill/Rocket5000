/* ==========================================================
   RENDER — pixel art helpers. Filled in Chunk 3.
   ========================================================== */
(function (global) {
  'use strict';
  global.Render = {
    drawPart() {},
    drawRocket() {},
    drawSky() {},
  };
})(window);
